"use strict";


// Error Model
// --------------
Sonar.Models.Error = Backbone.Model.extend({

    defaults: {
        "title": "Error",
        "error": ""
    }

    // This may get extended later but for now it's as simple as can be

});
